#!/bin/bash
# Skopeo is distributed via package managers, so no build step is required.
echo "[INFO] Skopeo does not require building. It will be installed via the system package manager during installation."
