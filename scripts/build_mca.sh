#!/bin/bash
# Build script for MCA (llvm-mca)
set -e
# MCA is provided by the system LLVM package, so no build step is needed

echo "[INFO] No build required for MCA (llvm-mca). Provided by system package."
